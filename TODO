volumes: read only?
service names and discovery?
handle deletion
cleanup on abort/cancel
network exists logic
service dependencies
project name parameter
compose file name parameter
issue with deploy while network being deleted
